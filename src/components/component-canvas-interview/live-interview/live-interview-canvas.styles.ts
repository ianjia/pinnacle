import { makeStyles } from '@fluentui/react-components';

export const useStyles = makeStyles({
  contentContainer: {
    overflowX: 'auto',
    overflowY: 'auto',
    padding: '16px',
  },

  actionContainer: {
    padding: '16px',
  }
});


